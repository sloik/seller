//system
import SwiftUI

struct MainView: View {
    var body: some View {
        VStack {
            Text("Future Main View of Application")
                .padding(.bottom, 24)
            Text("Stay tuned!")
        }
    }
}

#Preview {
    MainView()
}
